@model CarDIler.ViewModel.DetalCarViewModel

@{
    ViewData["Title"] = "Detal";
}

<div class="ftco-blocks-cover-1">
    <div class="ftco-cover-1 innerpage overlay" style="background-image: url('@Url.Content(Model.DetalCars.CoverPath)')">
        <div class="container">
            <div class="row align-items-center justify-content-center">
                <div class="col-lg-6 text-center">
                    <span class="d-block mb-3 text-white" data-aos="fade-up">@Model.DetalCars.Date</span>
                    <h1 class="mb-4" data-aos="fade-up" data-aos-delay="100">@Model.DetalCars.Name</h1>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="site-section">
    <div class="container">
        <div class="row">
            <div class="col-md-8 blog-content">
                <p class="lead"><h1>@Model.DetalCars.Name</h1></p>
                <hr />
                @if (ViewContext.HttpContext.User.IsInRole("Admin"))
                {
                    <p>
                        <span class="m-1"><a asp-action="EditCar" asp-controller="Admin" asp-route-id="@Model.DetalCars.Id"><img src="~/svg/edit.svg" width="25" height="25" title="Редагувати"></a></span>
                        <span class="m-1"><a asp-action="DelCar" asp-controller="Admin" asp-route-id="@Model.DetalCars.Id"><img src="~/svg/error.svg" width="25" height="25" title="Видалити"></a></span>
                    </p>
                    <hr />
                    @if (Model.DetalCars.Sold == true)
                    {
                        <p class="lead">Status <span class="font-weight-bold text-danger">Sold</span></p>
                    }
                    else
                    {
                        <p class="lead">Status <span class="font-weight-bold text-success">For Sale</span></p>
                    }
                    <hr />
                    <p class="lead">Price Netto <span class="">@Model.DetalCars.PriceNetto $</span></p>
                    <p class="lead">Price Brutto @Model.DetalCars.PriceBrutto $</p>
                    <p class="lead">Profit <span class="text-success">@Model.DetalCars.Profit $</span></p>
                    <hr />
                }
                <p><span class="lead">Brand</span> @Model.DetalCars.Brand.BrandName</p>
                <p><span class="lead">Year</span> @Model.DetalCars.Year</p>
                <p><span class="lead">Price</span> @Model.DetalCars.PriceBrutto $</p>
                <p><span class="lead">Engine</span> @Model.DetalCars.Engine</p>
                <p><span class="lead">Color</span> @Model.DetalCars.Color</p>
                <p><span class="lead">Distance</span> @Model.DetalCars.Distance км</p>
                <p><span class="lead">Vin code</span> @Model.DetalCars.Vin</p>
                <hr />

                <div class="container">
                    <div class="row">

                        @foreach (var photo in Model.CarImages)
                        {
                            <div class="col-lg-3 col-md-4 col-xs-6 thumb">
                                <a data-fancybox="gallery" href="@Url.Content(photo.Path)">
                                    <img class="img-fluid" src="@Url.Content(photo.Path)">
                                </a>
                            </div>
                        }
                    </div>
                </div>

                <hr />
                <p class="lead font-weight-bold">Detail</p>
                <p style="white-space: pre-line">@Model.DetalCars.Desc</p>
                <hr />
                <p><span class="lead">Date of publication:</span> @Model.DetalCars.Date</p>

                @if (Model.DetalCars.DateEdit != null && Model.DetalCars.DateEdit != Model.DetalCars.Date)
                {
                    <p><span class="lead">Date edit:</span> @Model.DetalCars.DateEdit</p>
                }

                <hr />
                <p class="lead font-italic">ID car @Model.DetalCars.Id</p>
            </div>
            <div class="col-md-4 sidebar">
                <div class="sidebar-box">
                    <img src="~/images/me_foto.jpg" alt="Free Website Template by Free-Template.co" class="img-fluid mb-4 w-50 rounded-circle">
                    <h3 class="text-black">@Model.DetalCars.AddByPosition</h3>
                    <p>@Model.DetalCars.AddByName @Model.DetalCars.AddBySurname</p>
                    <p>Arange for a test drive - call:</p>
                    <p>@Model.DetalCars.AddByPhoneNumber</p>
                </div>
            </div>
        </div>
    </div>
</div>
